[build-system]
requires = [
    "setuptools>=64",
    "wheel",
    "torch",
]
build-backend = "setuptools.build_meta"


[project]
name = "cumesh"
version = "0.0.1+rocm"
description = "Cuda mesh utils (ROCm)"
requires-python = ">=3.8"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "Jianfeng Xiang", email = "belljig@outlook.com" },
    { name = "Michael Lamothe", email = "michael.lamothe@gmail.com" }
]
dependencies = [
    "torch>=2.4.0",
    "tqdm",
]
